import { Text, View } from "react-native";

const SettingsPage = () => {
  return (
    <View className="flex-1 bg-background">
      <Text>settings</Text>
    </View>
  );
};

export default SettingsPage;
